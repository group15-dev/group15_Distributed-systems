#include <stdio.h>
#include "headers/server.h"


int main(int argc, char *argv[]){
/**
  * 
  
    putchar('H');
    
    FILE *file = fopen("output.txt", "w");

    if (file != NULL){
        putc('H', file);
    }
 */

    startServer();
    return(0);

}
